/*
package hongik.newswhale.application.service;

import hongik.newswhale.dto.ResponseArticle;

import java.util.Comparator;

// 최신순으로 뉴스 반환
class ResponseArticleComparator implements Comparator<ResponseArticle> {
    @Override
    public int compare(ResponseArticle responseArticle1, ResponseArticle responseArticle2) {
        if (responseArticle1.getArticleLastModifiedTime().before(responseArticle2.getArticleLastModifiedTime())) {
            return 1;
        } else if (responseArticle1.getArticleLastModifiedTime().after(responseArticle2.getArticleLastModifiedTime())) {
            return -1;
        }
        return 0;
    }
}
*/
